name:  1st_Workflow
on: [push]

jobs: 
    build:
        runs-on: ubuntu-latest

        steps: 
            - name: echo a string"
              run: echo "Executing 1st workflow"
            - name: Multiline Command
              run: |
                  node -v
                  npm -v